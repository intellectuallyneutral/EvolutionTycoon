pre-commit:
  parallel: false
  commands:
    format:
      glob: "*.{lua,luau}"
      run: stylua --check {staged_files}
    lint:
      glob: "*.{lua,luau}"
      run: selene {staged_files}
    asphalt-verify:
      run: asphalt sync
